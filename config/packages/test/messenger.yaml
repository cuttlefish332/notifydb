framework:
    messenger:
        transports:
            async: 'in-memory://'
            failed: 'in-memory://'
        routing:
            App\Message\SendInstantChangeEventEmailMessage: async
            Symfony\Component\Mailer\Messenger\SendEmailMessage: async
